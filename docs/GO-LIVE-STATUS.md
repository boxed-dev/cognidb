# CogniDB 3.0 — Go-Live Status

**Date:** 2026-07-15  
**Gatekeeper role:** final go-live audit (tests, coverage, build, release)  
**Repo version:** `3.0.1` (`cognidb.__version__`, `pyproject.toml`, `cognidb.client`)  
**Verdict:** **GO** (GitHub Release live; PyPI upload still pending / unknown until publish completes)

---

## Recommendation

**GO** — source, packaging, and GitHub Release `v3.0.1` are ready. No code blockers remain.

Residual work is **operational** (PyPI token/upload, profile pin), not library defects.

---

## Verification run (executed 2026-07-15)

| Check | Command / spot-check | Result |
|---|---|---|
| Git sync | `git fetch` + `main` vs `origin/main` | **PASS** — clean, HEAD = `origin/main` (`af0dc1a`) |
| Full suite | `pytest -q` | **PASS** — 114 passed, 0 failed, 1 skipped |
| Coverage gate | `pytest --cov=cognidb.security --cov=cognidb.pipeline --cov-fail-under=70` (+ `coverage report`) | **PASS** — TOTAL **80%** (fail-under 70) |
| Package import | `from cognidb import CogniDB` + `__version__` | **PASS** — `3.0.1` |
| Intent path | `from cognidb.intent.renderer import render_sql` | **PASS** |
| sdist/wheel | `python -m build` + `twine check dist/*` | **PASS** — `cognidb-3.0.1.tar.gz` + `cognidb-3.0.1-py3-none-any.whl` |
| GitHub Release | `gh release view v3.0.1` | **PASS** — live at https://github.com/boxed-dev/cognidb/releases/tag/v3.0.1 |
| README quickstart | Offline SQLite snippet vs live API | **PASS** — matches `SecureQueryPipeline` / `FakeSQLGenerator` |
| Watermarks | Scan for "Generated by" / "Cursor" / "Co-authored-by: AI" in docs | **PASS** — none found |
| Conflict markers | `<<<<<<<` / `=======` / `>>>>>>>` in source | **PASS** — none |
| PyPI `cognidb==3.0.1` | `pip index versions cognidb` | **PENDING** — registry still shows historical `0.2.x` (latest `0.2.5`); do not claim published until upload finishes |

---

## P0 criteria (ROADMAP / perfection exit)

| # | P0 criterion | Status | Evidence |
|---|---|---|---|
| 1 | `pip install cognidb` installs 3.x from PyPI matching Git tag | **PENDING** | GitHub `v3.0.1` live; PyPI still historical 0.2.x — human/sibling upload step |
| 2 | Public API stable + copy-paste SQLite quickstart (&lt;30 lines) | **PASS** | README quickstart + `examples/sqlite_offline_demo.py` |
| 3 | ≥80 automated tests incl. adversarial security suite | **PASS** | 114 passed; corpus under `tests/security/corpus/` |
| 4 | CI green on Python 3.10–3.12; coverage ≥70% on security + pipeline | **PASS** | CI matrix `3.10/3.11/3.12`; local coverage **80%** |
| 5 | Single execution path (no driver bypass of pipeline validation) | **PASS** | `CogniDB.query` → `SecureQueryPipeline.run` |
| 6 | Access control enforced or removed from docs (no theater) | **PASS** | Table + column allowlists wired; README matches `create_restricted_user` |
| 7 | Community health files complete | **PASS** | LICENSE, SECURITY.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md, issue + PR templates |
| 8 | README accuracy (feature claims have code + tests) | **PASS** | Honest PyPI caveat; extras match `pyproject.toml` (`dev`/`redis`/`mongo`) |

**P0 score for code / Git gate:** 7/8 pass; #1 awaits PyPI upload.

---

## Blockers

**None** for GO on source release / GitHub Release / local install.

---

## Human / sibling steps left (not code)

1. **PyPI token** — create/upload API token; `twine upload dist/cognidb-3.0.1*` (see [RELEASE-CHECKLIST.md](RELEASE-CHECKLIST.md)). Status unknown until that finishes.
2. **Profile pin** — pin the release / repo on the GitHub org or maintainer profile for discoverability.
3. Confirm `pip install cognidb==3.0.1` resolves from PyPI after upload (closes P0 #1).

GitHub Release for `v3.0.1` is **already published** (confirmed 2026-07-15).

---

## Counts snapshot

| Metric | Value |
|---|---|
| Tests passed | 114 |
| Tests failed | 0 |
| Tests skipped | 1 (Postgres without URL) |
| Security+pipeline coverage | 80% |
| Package version | 3.0.1 |
| Wheel / sdist | `cognidb-3.0.1-py3-none-any.whl` / `cognidb-3.0.1.tar.gz` |
| GitHub Release | https://github.com/boxed-dev/cognidb/releases/tag/v3.0.1 |
| PyPI | Not yet `3.0.1` (historical 0.2.x) |

---

## Out of scope / residual (non-blocking)

- Row-level security seam still aspirational (threat-model honest non-goal).
- Corpus depth can grow further; named tautology holes are covered.
- External dependents / community PRs are growth work, not release gates.
